# Windows Nginx

Dieser Bereich enthaelt das mitgelieferte Basis-Layout fuer `nginx` unter Windows.

Aktueller Stand:

- `nginx.conf` als Basisdatei
- virtuelle Tenant-Site wird zur Laufzeit als `tenant-site.conf` aus dem Installer generiert
- Aktivierung erfolgt ueber `activate-runtime.ps1`
