# Ubuntu Installer

Dieser Bereich enthaelt den nativen Ubuntu-Installer fuer `v1`.

Zielbild:

- Interaktives Setup-Script
- Preflight- und Pruefmodus
- `nginx`-Provisionierung
- Optionales `Let's Encrypt`
- `systemd` und `Supervisor` fuer Scheduler und Queue-Worker
