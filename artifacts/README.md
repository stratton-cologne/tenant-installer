# Artifacts

Dieser Ordner dient in der lokalen Entwicklungs- und Testphase als Ablage fuer vorbereitete Release-ZIP-Dateien.

Erwartetes Schema:

- `tenant-backend-<version>.zip`
- `tenant-frontend-<version>.zip`

Der Ubuntu-Installer staged diese Dateien zunaechst nach `APP_ROOT/installer/cache`.
